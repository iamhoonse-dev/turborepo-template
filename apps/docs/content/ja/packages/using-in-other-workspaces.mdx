# 🧑‍💻 パッケージの利用方法

他のワークスペースでパッケージを利用するには、以下の手順に従ってください。

## ➕ 依存関係の追加

まず、対象ワークスペースの`package.json`ファイルに依存関係を追加します。
例えば、`@repo/react-utils`パッケージを利用する場合は次のように記述します。

```json
{
  "dependencies": {
    "@repo/react-utils": "workspace:*"
  }
}
```

その後、依存関係をインストールします。

```bash
cd path/to/project/root
pnpm install
```

## 🧑‍💻️ 使用例

パッケージをインポートして利用できます。例えば、`@repo/react-utils`パッケージが提供する`useInterval`フックを使う例は以下の通りです。

```typescript filename="apps/web/containers/AutoCounter/index.tsx" {4,9}
"use client";

import { type FC, useReducer } from "react";
import { useInterval } from "@repo/react-utils/hooks";

const AutoCounter: FC = () => {
  const [counter, increase] = useReducer((state: number) => state + 1, 0);

  useInterval(increase, 1_000);

  return <>{counter}</>;
};
export default AutoCounter;
```
