import { Callout } from "nextra/components";
import LocalizedLink from "../../../../components/LocalizedLink";

# 📝 Empty Next App

The `apps/empty-next-app` workspace is a minimal template app based on Next.js.

<Callout type="info">
  This workspace is based on the default Next.js app included in the [Turborepo template](https://github.com/vercel/turborepo/tree/main/examples/basic). \
  **Reference source code link**: [Turborepo Next.js Template > apps > web](https://github.com/vercel/turborepo/tree/main/examples/basic/apps/web)
</Callout>

## 🎯 Main Purpose

- Provides the most basic structure to quickly start a Next.js project.
- Starts in a "blank" state with no unnecessary code or dependencies, making it ideal as a starting point for experiments or new projects.

<Callout type="info">
  For instructions on how to start a new Next.js app using this workspace, see:
  - <LocalizedLink href="/apps/add-new-app/add-next-app">`Applications > Add New App > Add a New Next App`</LocalizedLink>
</Callout>

## 🌟 Key Features

- Includes only the official default structure of Next.js.
- Does not include any UI components, styles, routes, or APIs.
- You can freely add any libraries or features as needed.
- ESLint and TypeScript settings are configured to inherit from the shared configuration.

<Callout type="info">
  For more details on the shared ESLint and TypeScript configurations inherited by this project, see the following two documents:
  - <LocalizedLink href="/configs/eslint-config">`Shared Config > ESLint Config`</LocalizedLink>
  - <LocalizedLink href="/configs/typescript-config">`Shared Config > TypeScript Config`</LocalizedLink>
</Callout>

## 🧑‍💻 Example Use Cases

- When you want to quickly create a new Next.js app
- When you need a sandbox app for experiments
- When you want to test or compare the Next.js environment within the monorepo

## 📌 Notes

- For the actual source code, refer to the `apps/empty-next-app` directory.
- Next.js documentation: [https://nextjs.org/docs](https://nextjs.org/docs)

